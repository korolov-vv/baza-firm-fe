# fly.toml app configuration file generated for baza-firm-fe on 2026-02-22T09:15:24Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'baza-firm-fe'
primary_region = 'fra'

# Runtime environment variables
# Set these using: flyctl secrets set KEY=value
# Example:
#   flyctl secrets set API_URL=https://api.example.com/api
#   flyctl secrets set KEYCLOAK_URL=https://auth.example.com
#   flyctl secrets set KEYCLOAK_REALM=bazafirm-customer
#   flyctl secrets set KEYCLOAK_CLIENT_ID=customer-app-web

[build]

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
