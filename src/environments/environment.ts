// Development environment configuration
// For local development, the app uses the /api/config endpoint
// which will use defaults if no environment variables are set
export const environment = {
  production: false
};